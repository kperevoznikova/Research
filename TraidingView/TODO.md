### Receiver scheme
```
(Get a list of cryptocurrency exchanges) => (Get a list of trade pairs) =>
(Select all shitcoins: shitcoin is a coin with a small life time, market capitalization and certainly traiding only on DEX'es) =>
(Make an .csv day timeframe dump) =>
(Send it to the classifier)
```
